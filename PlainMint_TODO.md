# PlainMint TODO

> 管理方式：按实现状态维护的唯一待办清单
> 对应需求：[`PlainMint_PRD.md`](PlainMint_PRD.md)
> 更新日期：2026-07-19
> 当前基线：`v0.3.2` + `6cde314`

---

## 使用规则

- 本文档管理尚未闭环的功能、质量和发布工作；PRD 只保留产品需求、验收口径与当前事实基线。
- 每项待办使用稳定 ID；PRD 的实现矩阵和验收前缺口以该 ID 关联，避免在两处维护同一份任务描述。
- 完成时勾选任务，并在同一行记录实现提交、验证命令或发布版本；随后更新 PRD 的实现矩阵。
- `[ ]` 为未完成，`[-]` 为进行中，`[x]` 为已完成。除非任务实际闭环，否则不得勾选完成。
- “候选项”不代表已承诺版本；开始实施前需要确认优先级、交互与验收范围。

## P0：可靠性与数据安全

- [ ] **TODO-R01｜解码失败后的编码重试与可操作保存错误**
  PRD：§8.16、§13.2、§13.4、§23.1
  完成条件：打开解码异常文件时可选择编码重试；保存失败说明原因并提供重试或另存为，正文与恢复副本不丢失。

- [ ] **TODO-R02｜恢复视图状态与异常场景验证**
  PRD：§8.5、§14.2、§23.2、§23.3、§24.3
  完成条件：会话和恢复数据保存并恢复每个面板的光标、选区和滚动位置；覆盖损坏会话、系统关机、强制结束和磁盘空间不足。

- [ ] **TODO-R03｜跨平台外部变化与同步冲突完善**
  PRD：§8.15、§23.7、§24.1
  完成条件：macOS 接入目录事件监听；验证 OneDrive/iCloud/Dropbox 的替换、移动、删除和恢复；评估并实现安全的三方合并策略或明确降级交互。

- [x] **TODO-R04｜最近文件的完整管理**
  PRD：§8.13、§22.1
  已完成：欢迎页与主界面工具栏下拉的快速打开、单项移除和清空操作；加载最近文件后检查文件元数据，在欢迎页和工具栏中标记已不存在的文件，并在打开前再次校验。
  完成条件：为失效文件提供打开前的明确标记，并保留已完成的欢迎页和工具栏快速打开体验。
  实现：`4b96aa3`；验证：`npm run check`（109 tests）。

- [ ] **TODO-R05｜保存目录与云同步目录完善**
  PRD：§8.6、§23.7
  完成条件：支持重置到系统文档目录，并在配置目录失效时提供明确的回退提示。

## P1：编辑体验与可用性

- [ ] **TODO-E01｜搜索选择增强**
  PRD：§8.1.4、§8.10.2、§22.2
  完成条件：选中内容可一键带入查找栏；提供“选择全部匹配”命令并支持多光标编辑；与正则、只读、分屏和撤销语义一致。

- [ ] **TODO-E02｜特殊字符显示开关**
  PRD：§8.1.4、§22.2
  完成条件：在编辑器设置中提供特殊空白/不可见字符显示开关，默认值经过普通写作与日志查看验证，设置即时生效并持久化。

- [ ] **TODO-E03｜文本统计精度与扩展**
  PRD：§8.11、§23.5
  完成条件：字符统计正确处理组合字符和 ZWJ emoji；评估并实现单词、段落、空格、中文字数和阅读时间中的高价值项目。

- [ ] **TODO-E04｜撤销历史质量与资源边界**
  PRD：§8.12、§17.3
  完成条件：连续输入按合理语义合并；历史同时受操作数与内存上限限制；大文本和分屏同文档保持可预测的撤销行为。

- [ ] **TODO-E05｜按文档保存视图偏好**
  PRD：§8.8、§14.2
  完成条件：自动换行可按文件或文件类型记忆；不影响当前全局默认设置与会话恢复。

- [ ] **TODO-E06｜自动缩进评估**
  PRD：§8.1.3、§8.1.4
  完成条件：仅在明确的纯文本/配置文件规则下提出交互方案；默认不能向普通文本、日志或既有文件引入非预期空白。

## P2：质量、更新与发布

- [x] **TODO-Q01｜CI 覆盖 Rust 验证**
  PRD：§24、§25
  完成条件：CI 运行 `cargo fmt --check`、Rust 编译和 Rust 单元测试，并能清晰定位前端或 Rust 失败阶段。
  已完成：前端与 Rust 验证拆分为独立 CI 任务；Rust 任务安装 Tauri Linux 构建依赖后运行格式检查、编译检查和单元测试。
  实现：`71c76d8`；验证：`cargo fmt --check`、`cargo check --locked`、`cargo test --locked`（17 tests）。

- [ ] **TODO-Q02｜端到端与跨平台回归测试**
  PRD：§23、§24
  完成条件：覆盖文件读写、备份恢复、会话、外部冲突、Windows 安装升级与 macOS 核心流程；高风险路径具备可重复的手工验收记录。

- [ ] **TODO-Q03｜无障碍与响应式人工验收**
  PRD：§19、§23.9、§24.4
  完成条件：完成屏幕阅读器、纯键盘、高 DPI、窄窗口和减少动画的 Windows/macOS 实机验收并记录问题处理结果。

- [ ] **TODO-U02｜更新体验与 Windows 升级验收**
  PRD：§8.17、§23.8、§25.3
  完成条件：支持跳过指定版本；完成 Windows 实机升级、签名验证失败和未保存内容保护的端到端验证。

- [ ] **TODO-U03｜macOS 自动更新产物**
  PRD：§8.17、§25.2、§25.3
  完成条件：发布已签名的 macOS 更新压缩包、`darwin-*` 更新清单并完成升级验证。

- [ ] **TODO-R06｜正式发布信任链**
  PRD：§18、§25.1、§25.2
  完成条件：配置 Windows Authenticode、macOS Developer ID 与公证；验证安装和升级时的系统信任提示符合发布要求。

## 候选项：V1.1 及以后

以下项目尚未进入承诺排期；它们对应 PRD 的版本范围，只有评审后才提升为 P0–P2 任务。

| ID | 候选功能 | PRD 对应 |
|---|---|---|
| TODO-B01 | 文件编码手动转换 | §22.2 |
| TODO-B02 | 只读模式 | §22.2 |
| TODO-B03 | 超大文件优化 | §13.6、§17、§22.2 |
| TODO-B04 | 固定 Tab | §14.3、§22.2 |
| TODO-B05 | 自定义快捷键 | §8.18、§22.2 |
| TODO-B06 | 文件关联优化 | §22.2 |
| TODO-B07 | 更完善的恢复中心 | §8.15、§22.2 |
| TODO-B08 | 多窗口、快速文件切换器、命令面板 | §22.3 |
| TODO-B09 | Markdown 预览、自定义主题导入、云服务 API 直连 | §22.3 |

## 已完成记录

- [x] **可维护文档模板**：工具栏和欢迎页提供模板选择器，内置六种纯文本模板；设置可分别编辑内置模板的中英文正文、新增或删除自定义模板，并在本机模板目录中维护 `.pmtpl` 文件。
  PRD：§8.1.8、§23.1、§24.2；实现：本提交；验证：`npm run check`、`npm run build`、`cargo test`。

- [x] **退出时保留未命名草稿**：关闭应用时，未命名且含内容的文档静默保存到内部会话草稿，不显示保存确认；已有路径文件仍按原有确认流程处理，最终会话写入失败时阻止关闭并保留内容。
  PRD：§5.1、§8.2.4、§8.5、§12.1、§13.5、§23.2、§24.3；实现：本提交；验证：`npm run check`、`npm run build`。

- [x] **工具栏、文件拖入与窗口操作闭环**：标题栏仅保留品牌和窗口控制；工具栏复用现有保存、会话、查找、分屏与设置逻辑，并通过自适应省略号菜单保留窄窗口访问；支持拖入文件到目标面板和字号快捷键。
  PRD：§6、§7.3、§8.14、§8.18；实现：`6cde314`；验证：`npm run check`（118 tests）、`npm run build`、`cargo check --locked`。

- [x] **复合下拉工具栏按钮**：新建、打开、保存改为图标与功能名加独立下拉箭头；模板、最近文件和另存为分别收纳到对应下拉菜单，打印及其他辅助操作保持独立图标，设置仍固定在最右侧。
  PRD：§6、§7.1、§7.3；实现：本提交；验证：`npm run check`、`npm run build`、最小窗口布局验收。

- [x] **轻量文本整理**：在“工具栏 → 小工具”提供稳定自然排序、精确去重、删除空白行和清除行尾空格；支持完整行选区、多选区合并、全文回退、一次撤销、只读保护和分屏同步。
  PRD：§8.1.4、§8.1.7、§23.1、§24.2；实现：`6cde314`；验证：`npm run check`（118 tests）、`npm run build`。

- [x] **系统打印**：通过工具栏“打印”和 `Ctrl/Cmd+P` 打印活动文档的最新内存纯文本内容；打印不改变正文、未保存状态、撤销、会话或磁盘文件。
  PRD：§6、§8.18、§8.20、§23.10、§24.5；实现：`6cde314`；验证：`npm run check`（118 tests）、`npm run build`。

- [x] **在已打开文档中查找**：查找栏支持当前文档和已打开文档范围；已打开范围搜索会话内存内容，按文档展示准确计数、行号和上下文，最多显示前 500 项；支持未保存、未命名和分屏去重，结果可激活既有 Tab 并定位选中匹配，替换仍限定当前文档。
  PRD：§8.10.6、§23.5；实现：`95cb10d`；验证：`npm run check`（114 tests）、`npm run build`。

- [x] **Tauri CSP 与 CodeMirror 样式兼容**：复用 Tauri 为打包样式注入的 CSP nonce，并传递给普通编辑器、冲突对比和分屏文件对比中的全部 CodeMirror 实例；发布版保留现有 CSP，恢复行号、正文、光标、选区、主题和语法高亮的完整布局。
  PRD：§8.1、§8.1.4、§17.2；实现：本提交；验证：`npm run check`（108 tests）、`npm run build`、`cargo fmt --check`、`cargo test`（17 tests）、标准 CSP 的 Tauri Release 构建与实机布局验收。

- [x] **启动交互性能与标题栏可靠性**：HTML 首帧提供独立的原生拖动标题栏；首屏优先加载可操作的窗口框架，CodeMirror 编辑器、语法高亮与对比视图在空闲期或用户点入编辑区时再加载；文件、会话、备份及注册表命令通过 Tauri 异步响应器离开原生窗口事件线程；系统右键菜单查询延迟到打开设置后，首次会话回写和备份清理移出启动关键窗口；自绘无边框标题栏保留原生拖动和双击最大化。
  PRD：§3.2、§7.2、§17.1；实现：`9761d80`、`8e10464`、本提交；验证：`npm run check`（105 tests）、`npm run build`、`cargo fmt --check`、`cargo test`（17 tests），并完成空配置及真实会话副本的 release 冷启动响应探测。

- [x] **仅手动检查应用更新**：移除启动后的自动更新检查和设置项；仅在“关于与更新”页面手动点击后请求更新服务，结果在页面或更新对话框中展示。
  PRD：§3.1、§8.17、§10.8、§22.1、§23.8；实现：`34e92cc`；验证：`npm run check`（103 tests）、`npm run build`。

- [x] **设置项悬停说明**：在文件与目录、备份与恢复、语言设置旁提供悬停显示的小图标说明，不提供独立帮助页。
  PRD：§10.9；实现：本提交；验证：`npm run check`、`npm run build`。

- [x] **未命名文档序号命名**：新建文档统一使用“未命名 N”，首次保存建议 `untitled-N.txt`；标签、提示、分屏对比和会话恢复保持一致，并兼容既有未命名会话。
  PRD：§8.1.5；实现：本提交；验证：`npm run check`、`npm run build`。

- [x] **文本与代码语法高亮**：支持状态栏手动语言模式和文件名优先、内容辅助的自动识别；覆盖 19 个语言系列，手动选择随工作区会话保存。新建无扩展名文件在空闲后仅识别一次；大于 5 MiB 或 50,000 行时暂停解析。对比与冲突对比复用同一语言支持。
  PRD：§8.1.4；实现：本提交；验证：`npm run check`（88 tests）、`npm run build`、`cargo fmt --check`、`cargo test`（17 tests）。

- [x] **跨平台品牌化主界面优化**：建立统一的 PlainMint Minimal 视觉语言，重构标题栏、工具栏、标签页、编辑区、查找替换栏、通知栏、状态栏、欢迎页与弹出菜单的紧凑布局；保留现有功能、组件和数据接口。
  PRD：§7.2、§7.3；实现：`0ee6360`；验证：`npm run check`（80 tests）、`npm run build`、`cargo fmt --check`、`cargo test`（17 tests），并完成 1280×800、1024×700、720×560 明暗主题响应式验收。

- [x] **CodeMirror 第一批增强**：跳转行、多光标选择下一个匹配、选中词匹配高亮、正则捕获组替换、拼写检查开关和快捷键速查。
  PRD：§8.1、§8.10、§8.18；实现：`f930676`；验证：`npm run check`（71 tests）、`npm run build`。

- [x] **文本文件对比**：仅在分屏模式下对比左右活动文档的当前内容（含未保存修改），提供只读双栏差异、循环导航、编码与换行符信息、大文件降级和快捷键入口。
  PRD：§8.1.4、§8.18、§8.19、§22.2、§24.2；实现：`7428714`、`eef5e2e`；验证：`npm run check`（76 tests）、`npm run build`、`cargo fmt --check`、`cargo test`（17 tests）。

- [x] **设置导航与关于更新整合**：将快捷键说明独立为关于页之前的设置页面；关于页展示当前版本、作者 Sunky 与官网，并承载手动更新检查和持久化检查结果。
  PRD：§8.17、§8.18、§10.7、§10.8；实现：`c246ea3`；验证：`npm run check`（78 tests）、`npm run build`、`cargo fmt --check`、`cargo test`（17 tests）。
